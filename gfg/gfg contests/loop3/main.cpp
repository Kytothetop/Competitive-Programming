#include <bits/stdc++.h>

using namespace std;
int countValues (int n)
{
   for(int i =0 ; i < n ; i++)
   {
//       if(n^i == )

   }
}
int main()
{
    int n = 12;
//    cout << (1 ^ 12);
    cout << countValues(n) << endl;
    return 0;
}
