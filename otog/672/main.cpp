#include <iostream>

using namespace std;

int n;
int
vector<int> arr(n);
void sol(vector<int> ff)
{
	for()
}
int main()
{
    int k;
    cin >> n >> k;
    for(int i =0; i < n ; i++)
        {
            arr[i] = i+1;
        }
    while(k--)
        {
            vector<int> ff(n);
            for(int i =0; i < n ; i++)cin >> ff[i];
            sol(ff);
        }

}
