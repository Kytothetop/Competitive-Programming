#include <iostream>

using namespace std;
int num(int lim)
{
	return rand()%lim;
}
int main()
{
	ios_base::sync_with_stdio(0);cin.tie(0);
	srand(time(NULL));
    int n;
    cin >> n;
	priority_queue <pair<int,int>> pq;
	for(int i =0; i < n ; i++)
	{

	}
	pq.push({0,s});

}
