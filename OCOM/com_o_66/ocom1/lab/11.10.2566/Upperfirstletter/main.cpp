#include <bits/stdc++.h>

using namespace std;
//Use Ctrl + Z to exit program
int main()
{
    string str;
    while(cin >> str)
        {
            str[0] = toupper(str[0]);
            cout << str << '\n';
        }
}
