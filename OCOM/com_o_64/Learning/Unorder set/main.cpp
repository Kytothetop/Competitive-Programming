#include <iostream>
#include <bits/stdc++.h>

using namespace std;

int main()
{
    unordered_set<int> u;
    u.insert(10);
    u.insert(20);
    u.insert(5);
    for(auto & e : u)
    {
        cout << e << endl;
    }
    cout << "Via iterator.\n";
    unordered_set<int> t
}
